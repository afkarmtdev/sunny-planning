# Copy to .env.local and fill in from your Supabase project settings (Project Settings > API).
# When these are missing the app runs in local demo mode with seeded data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
